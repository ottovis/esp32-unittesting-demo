#include <gpio_interface.h>
#include <assert.h>

int gpio_interface::set_pin(int32_t pin, bool value)
{
    assert(0);
};
int gpio_interface::get_pin(int pin)
{
    assert(0);
};
int gpio_interface::set_direction(int pin, gpio_direction_e direction)
{
    assert(0);
};